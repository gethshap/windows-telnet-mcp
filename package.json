{
  "name": "windows-telnet-mcp",
  "version": "0.1.0",
  "description": "MCP server for controlling a real, visible Windows Telnet client session.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "4.5.4"
  }
}
